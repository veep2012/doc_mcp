[pytest]
testpaths = tests
pythonpath = src
addopts = -ra -m "not smoke"
asyncio_mode = auto
markers =
    smoke: end-to-end smoke coverage that may require local prerequisites
    crawl_smoke: smoke coverage for crawl/index flows
    mcp_smoke: smoke coverage for MCP stdio flows
