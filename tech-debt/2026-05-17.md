# Tech Debt - 2026-05-17

- Optional convenience flow: allow `docmcp-crawl` to trigger `docmcp-vectorize` as an explicit opt-in follow-up step without moving vector writes into the MCP query runtime.
