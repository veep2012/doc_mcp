# Technical Debt

Deferred implementation concerns identified on 2026-08-03.

## TD-20260803-01 — Await authentication coroutine in configuration-loader test

- **Context:** `make test` passed after fixing PDF crawling compatibility, but pytest emitted a runtime warning that `authenticate` was never awaited.
- **Location:** `tests/test_config_loader.py`; authentication callback handling in the configuration-loader test path.
- **Impact/Risk:** The warning can hide an actual async-resource leak or allow the test to pass without exercising the intended authentication callback behavior.
- **Why Deferred Now:** It is non-blocking and unrelated to the PDF crawling failures fixed in this task.
- **Proposed Fix:** Inspect the test fixture/callback mock and await or explicitly consume the authentication coroutine, then assert the intended callback contract.
- **Acceptance Signal:** The full test suite completes without the `coroutine 'authenticate' was never awaited` warning.
- **Status:** open
