# Technical Debt

Deferred implementation work captured during review and maintenance.

## TD-20260524-01
- Title: Add an installed-wheel integration test for console entry points
- Context: While expanding coverage for the vectorizer and crawl/vectorize workflow, the current tests verified `pyproject.toml` script declarations but did not exercise the installed command path in a wheel-style environment.
- Location: `pyproject.toml`, `tests/test_packaging.py`, `tests/test_vectorize_cli.py`, `tests/test_crawl_cli.py`
- Impact/Risk: The entry points could remain correct in source but still break after packaging or installation changes, especially for the new `docmcp-vectorize` and `docmcp_vectorizer` commands and the chained crawl/vectorize workflow expectations.
- Why Deferred Now: The current task focused on source-tree coverage and behavior fixes; an installed-wheel integration test requires a packaging/runtime harness that is broader than the current unit-test scope.
- Proposed Fix: Add one integration-style test that builds or installs the wheel into an isolated environment, then invokes the installed console scripts and verifies the expected CLI behavior and command aliases.
- Acceptance Signal: A wheel-installed test run proves `docmcp-vectorize`, `docmcp_vectorizer`, and `docmcp-crawl --vectorize` resolve and execute correctly from the installed distribution.
- Status: open

## TD-20260524-02
- Title: Write test scenarios in markdown before implementing automated tests
- Context: While expanding vectorizer and crawl coverage, several tests were added directly in code without a dedicated markdown scenario document first.
- Location: `tests/`, `documentation/manual-test-scenarios.md`, `documentation/`
- Impact/Risk: Test intent can drift from product expectations, review feedback becomes harder to apply consistently, and future coverage work may miss important scenarios or duplicate effort.
- Why Deferred Now: The current task was focused on closing concrete coverage gaps; introducing a new test-design workflow would require repository process changes and retrofitting existing scenarios.
- Proposed Fix: Define each new test scenario in a markdown document first, review and refine the scenario there, then implement the automated test from that approved scenario.
- Acceptance Signal: New coverage work starts with a markdown scenario entry and the corresponding test implementation can be traced back to that scenario document.
- Status: open
