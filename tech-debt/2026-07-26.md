# Technical Debt

Deferred technical debt items recorded on 2026-07-26.

## TD-20260726-01: Restore pytest asyncio configuration support

- **Context:** A version-bump verification run reported that pytest does not recognize the configured `asyncio_mode` option.
- **Location:** `pytest.ini`, test environment dependencies/configuration.
- **Impact/Risk:** Async test behavior may not be configured as intended, and the warning can hide other test-configuration problems.
- **Why Deferred Now:** The warning is unrelated to the requested package version bump and all targeted tests passed.
- **Proposed Fix:** Ensure the pytest asyncio plugin is installed in the project test environment, or remove/update the stale `asyncio_mode` configuration if async tests no longer require it.
- **Acceptance Signal:** Pytest recognizes `asyncio_mode` without warnings and async tests execute under the intended mode.
- **Status:** open

## TD-20260726-02 — Make optional-dependency tests self-contained

- **Context:** Full-suite verification for the optional Playwright/MCP collection-gate change found tests that fail during execution when optional dependencies are absent.
- **Location:** `tests/test_auth_cli.py`, `tests/test_crawl_cli.py`, `src/docmcp/crawl_cli.py`
- **Impact/Risk:** Minimal or partially installed test environments report failures instead of consistently skipping or using a test double, obscuring the actual test result.
- **Why Deferred Now:** These failures predate and are outside the requested collection-gate regression scope; changing their dependency setup or PDF test seams would broaden this documentation/test-support change.
- **Proposed Fix:** Add targeted dependency gates or stable module seams for the Playwright browser-error tests and pypdf extraction tests, then verify the full suite without those packages installed.
- **Acceptance Signal:** The full suite completes with no unexpected failures in an environment missing Playwright and pypdf; only explicitly gated tests are skipped.
- **Status:** open
